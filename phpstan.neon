includes:
    - ./vendor/larastan/larastan/extension.neon

parameters:

    paths:
        - app

    # The level 10 is the highest level
    level: 8

    excludePaths:
        # Framework scaffolding — no domain logic; excluded from the strict sweep.
        - app/Http/Middleware
        - app/Providers

    checkModelProperties: true
